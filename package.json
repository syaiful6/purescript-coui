{
  "private": true,
  "files": [],
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "example-counter": "bower link && cd examples/counter && bower link purescript-coui && bower install && npm run build",
    "example-todos": "bower link && cd examples/todos && bower link purescript-coui && bower install && npm run build"
  },
  "dependencies": {
    "react": "^0.14.8",
    "react-dom": "^0.14.6"
  }
}
